# NOD–MARL 方法级技术方案

> 本方案用于指导 Codex 实现。它只规定方法、数学结构、阶段依赖、验证内容和验收边界，不规定编程语言、框架、目录、类接口或网络的具体写法。

## 1. 总体目标

实现一套用于多车无显式通信交互的分布式控制框架：每辆车根据本地观测和邻车运动历史，形成连续、有界、可校准、可纠正的合作意见；意见进入本地策略，并通过任务、安全、死锁三类不同的 Value 通道影响控制动作。

整体链路为：

\[
\boxed{
\text{局部观测}
\rightarrow
\text{动态交互图}
\rightarrow
\text{成对历史与证据}
\rightarrow
\text{KL 结构保持意见}
\rightarrow
\text{多邻居聚合}
\rightarrow
\text{分布式 Actor}
\rightarrow
\text{三类约束 Value 训练}
}
\]

保持两项核心创新不变：

1. 用 Bernoulli–Fisher 几何上的自由能动力学表示合作意见；
2. 用任务、安全、死锁三类不同 Bellman 结构约束长期控制。

## 2. 理论环境与适用边界

理论部分统一采用确定性闭环：

\[
y_{t+1}=F(y_t,\mathbf u_t),
\qquad
\mathbf u_t=\Pi(y_t).
\]

增广状态写为：

\[
y_t=(s_t,Z_t,H_t,\zeta_t,\mathcal G_t),
\]

其中：

- \(s_t\) 是物理状态；
- \(Z_t\) 是所有有向交互边的意见；
- \(H_t\) 是形成意见所需的历史状态；
- \(\zeta_t\) 是死锁判定所需的计时、进展和许可状态；
- \(\mathcal G_t\) 是动态交互图。

必须明确以下边界：

- 理论上的路径约束保证依赖确定性闭环、可行初态、精确 Value 和精确动作求解；
- 神经网络 Critic 和 Actor 只能提供近似约束满足，不能直接称为严格安全证书；
- 随机扰动、感知噪声和模型误差作为鲁棒性实验处理，不自动继承确定性保证；
- 死锁约束只排除被明确定义的死锁集合，不等价于一般性的最终到达保证。

## 3. 局部交互表示

### 3.1 动态有向交互图

每辆车 \(i\) 仅为具有实际交互可能的邻车 \(j\) 建立有向边 \(i\leftarrow j\)。边的选择应综合：

- 感知可见性；
- 几何距离；
- 路径或冲突区重叠；
- 相对速度和闭合趋势；
- TTC、PET 或预计占用时窗；
- 车道、换道、汇入、交叉口等交互关系。

图应随时间变化，但同一邻车在连续帧中需要保持身份一致，以维护其历史状态和意见。新出现的边使用中性意见初始化；短时遮挡可以保留状态，长期消失后释放。

### 3.2 成对特征

成对特征应以本车局部坐标表达，至少覆盖：

- 相对位置、速度、加速度和航向；
- 双方到冲突点的距离、预计到达时间及其差值；
- 冲突区占用状态；
- 道路拓扑和优先权；
- 信号或通行许可；
- 当前风险、运动趋势和观测置信度。

所有特征必须有明确物理含义和方向约定。缺失信息应显式标识，不能与数值零混淆。

## 4. 历史、证据与风险注意力

### 4.1 历史编码

对每条边维护历史表示：

\[
h_{ij,t}=\mathcal H(h_{ij,t-1},\chi_{ij,t}).
\]

它用于表达单帧无法观察到的让行趋势、持续加减速、冲突接近与解除过程。所有同构交互边共享历史编码规律。

### 4.2 新证据

从相邻时刻变化中构造证据 \(\xi_{ij,t}\)，优先使用可解释的物理变化量，例如：

- 邻车是否减小冲突闭合速度；
- 是否增加双方到达冲突点的时间间隔；
- 是否降低未来冲突区同时占用概率；
- 是否通过减速、偏转或停止产生风险缓解。

证据分量应统一方向：数值越大，越支持“邻车正在缓解冲突风险”。

### 4.3 风险注意力

风险注意力 \(A_{ij,t}\in[0,1]\) 决定系统是否需要从中性状态进入明确意见分支。令 \(a>1\)，临界点为：

\[
A_c=\frac1a.
\]

方法上要求：

- 低风险交互倾向满足 \(A<A_c\)；
- 冲突迫近时倾向满足 \(A>A_c\)；
- \(A\) 随风险连续变化；
- \(A\) 由当前和历史物理信息决定，不使用未来标签。

风险注意力与多邻居消息注意力是两个不同概念，不能混用。

## 5. 连续合作意见

### 5.1 意见含义

定义：

\[
\rho_{ij,t}=\frac{1+z_{ij,t}}2,
\qquad
z_{ij,t}\in(-1,1).
\]

其中 \(\rho\) 表示车辆 \(i\) 根据本地证据对“车辆 \(j\) 将采取风险缓解行为”的连续信念。正意见对应更强的风险缓解倾向，负意见对应更弱或更具竞争性的行为，中性意见为 \(z=0\)。

### 5.2 先验势能

采用：

\[
U_A(z)=\frac12z^2-\frac A a\log\cosh(az).
\]

其作用为：

- 低风险时将意见恢复到中性；
- 高风险时使中性状态失稳；
- 产生正负两个稳定意见分支；
- 保留意见随风险连续变化的结构。

在中性证据下：

\[
U_A''(0)=1-aA,
\]

因此 \(A_c=1/a\)。加入非中性观测似然后，一般临界条件应写成：

\[
\mathcal F_t''(0)=0,
\]

不能无条件继续把 \(1/a\) 当作完整自由能的精确临界点。

### 5.3 观测自由能

定义：

\[
\mathcal F_t(z)
=U_{A_t}(z)
-\lambda_{\mathrm{obs}}A_t
\log p_\phi(\xi_t\mid h_t,z).
\]

其中：

- 先验势能提供中性恢复和分岔；
- 观测似然根据实际运动证据推动意见；
- \(A\) 决定当前交互的重要程度；
- \(\lambda_{\mathrm{obs}}\) 调节结构先验与观测证据的相对权重。

首版应优先采用关于 \(z\) 具有可控曲率的似然形式，使 \(\partial_z\log p\) 和 \(\partial_{zz}\log p\) 可稳定获得，并能验证 KL 子问题的唯一性。不要在首版使用曲率完全不受约束的任意似然网络。

### 5.4 连续动力学

\[
\boxed{
\tau_z\dot z
=-(1-z^2)\partial_z\mathcal F_t(z)
}
\]

其中 \(1-z^2\) 来自 Bernoulli 概率空间在 \(z\) 坐标下的 Fisher 几何。

当 \(A,h,\xi\) 冻结时：

\[
\dot{\mathcal F}
=-
\frac{1-z^2}{\tau_z}
(\partial_z\mathcal F)^2
\le0.
\]

观测随时间变化时只能写成：

\[
\frac{d\mathcal F_t}{dt}
=\partial_t\mathcal F_t
-\frac{1-z^2}{\tau_z}
(\partial_z\mathcal F_t)^2,
\]

因此不宣称时变观测下总自由能必然单调下降。

## 6. KL 结构保持离散更新

实际控制周期采用：

\[
z_t=
\arg\min_{z\in[-1+\varepsilon_z,1-\varepsilon_z]}
\left[
\mathcal F_t(z)
+\frac{\tau_z}{\Delta t}
D_{\mathrm{KL}}
\left(
\operatorname{Ber}\frac{1+z}{2}
\middle\|
\operatorname{Ber}\frac{1+z_{t-1}}2
\right)
\right].
\]

使用紧区间而不是开区间，以保证在连续目标下解存在。内部解满足：

\[
G(z)=
\frac{\tau_z}{\Delta t}
[\operatorname{atanh}(z)-\operatorname{atanh}(z_{t-1})]
+\mathcal F_t'(z)=0.
\]

若：

\[
\mathcal F_t''(z)
+\frac{\tau_z}{\Delta t(1-z^2)}
\ge m>0,
\]

则目标严格凸，解唯一。实现方法应满足：

- 一维求根带区间保护；
- 能识别内部解和边界解；
- 输出求根残差、曲率下界和收敛状态；
- 内部解使用隐式微分；
- 边界解只声明分段可微；
- 未收敛不能静默忽略，应有确定性回退策略。

内部解的隐式梯度为：

\[
\frac{\partial z^\star}{\partial\theta}
=-
\frac{\partial_\theta G(z^\star)}
{\partial_zG(z^\star)}.
\]

首版应把曲率条件作为硬检查，而不是只加入软曲率损失。

## 7. 意见语义锚定

### 7.1 未来风险缓解目标

定义：

\[
g_{ij,t}^{(j)}
=R_{ij,t:t+H}^{\mathrm{cf}}
-R_{ij,t:t+H}^{\mathrm{actual}}.
\]

反事实轨迹将车辆 \(j\) 替换为恒速或恒控制模型。若实际风险小于反事实风险，则 \(g>0\)，表示车辆 \(j\) 的真实行为产生了风险缓解。

软标签为：

\[
y_{ij,t}
=\sigma(k_g(g_{ij,t}^{(j)}-\delta_g)).
\]

反事实标签仅用于训练和评估，不进入部署控制输入。对无法可靠构造反事实的样本应标为无效，而不是当作零贡献。

### 7.2 校准与单调性

校准目标：

\[
\mathcal L_{\mathrm{cal}}
=-y\log\rho-(1-y)\log(1-\rho).
\]

同时要求：

\[
\frac{\partial}{\partial z}
\mathbb E_\phi[g\mid h,z]
\ge0.
\]

优先通过单调结构保证这一性质；若采用一般函数，再使用单调惩罚。这样可以阻止 \(z\) 在联合训练中退化为无语义的隐藏状态。

应持续监控：

- Brier score、NLL 和校准误差；
- \(z\) 与未来风险缓解的相关性；
- 错误初始意见的纠正速度；
- 联合训练前后语义指标是否退化。

## 8. 多邻居聚合与分布式策略

每条边形成消息：

\[
e_{ij,t}=\psi(\chi_{ij,t},h_{ij,t},A_{ij,t},z_{ij,t}).
\]

再以邻居维度的软注意力聚合：

\[
\alpha_{ij,t}=\operatorname{softmax}_j g(e_{ij,t}),
\qquad
m_{i,t}=\sum_j\alpha_{ij,t}e_{ij,t}.
\]

本地策略输入为：

\[
b_{i,t}=[o_{i,t},m_{i,t},u_{i,t-1}],
\]

输出名义连续动作：

\[
u_{i,t}^{\mathrm{nom}}=\pi_\theta(b_{i,t}).
\]

方法要求：

- 同构车辆共享 Actor 参数；
- 聚合对邻居排列不敏感；
- 无邻居时有确定的零消息行为；
- 上一步动作进入策略以提高控制连续性；
- 动作幅值和变化率在训练与部署中采用一致处理；
- Critic 评价的是最终实际执行动作，而非未经约束的名义动作。

联合策略保持分布式结构：

\[
\Pi_\theta(\mathbf u_t\mid B_t)
=\prod_i\pi_\theta(u_{i,t}\mid b_{i,t}).
\]

## 9. 安全约束定义

即时安全违反量为：

\[
g_s(y_t)=\max_m h_s^{(m)}(y_t),
\]

其中所有约束统一使用“非正为满足”的 signed margin。可包含：

- 车辆间动态安全距离；
- 碰撞集合；
- 道路和可行驶区域边界；
- 有效闭合情形下的 TTC 下界。

不同物理量应先进行合理尺度归一化，再取最大值。动作上下限和动作变化率可由动作生成过程直接强制，不必混入状态安全量。

安全 Value 为：

\[
V_s^\Pi(y_t)=\sup_{k\ge t}g_s(y_k).
\]

在确定性精确条件下：

\[
V_s^\Pi(y_0)\le0
\iff
g_s(y_k)\le0,\quad\forall k\ge0.
\]

## 10. 死锁约束定义

死锁是时序性质，必须由增广状态 \(\zeta_t\) 描述。判定至少同时考虑：

- 冲突关系仍存在；
- 一组相关车辆持续低速；
- 在时间窗口内没有有效路线进展；
- 当前规则或信号允许至少一方前进；
- 存在安全可行的解除动作。

只有这些条件持续满足超过阈值，才进入所定义的死锁集合。由此构造：

\[
g_d(y_t)>0
\]

表示已进入死锁集合，并定义：

\[
V_d^\Pi(y_t)=\sup_{k\ge t}g_d(y_k).
\]

正常红灯等待必须因“当前不允许前进”而被排除；不存在任何安全解除动作的物理困境也应与策略性死锁区分。

## 11. 三类 Value 与 Bellman 结构

任务 Value：

\[
V_\ell^\Pi(y_t)
=\sum_{k=t}^{\infty}\gamma^{k-t}
\ell(y_k,\mathbf u_k).
\]

任务成本可包含时间、路径偏差、能耗、舒适性和动作变化率。

三类 Action-Value：

\[
Q_\ell^\Pi(y_t,\mathbf u_t)
=\ell_t+\gamma V_\ell^\Pi(y_{t+1}),
\]

\[
Q_s^\Pi(y_t,\mathbf u_t)
=\max\{g_s(y_t),V_s^\Pi(y_{t+1})\},
\]

\[
Q_d^\Pi(y_t,\mathbf u_t)
=\max\{g_d(y_t),V_d^\Pi(y_{t+1})\}.
\]

三条通道必须独立训练：

- 任务通道使用折扣加法递推；
- 安全和死锁通道使用最大值递推；
- 约束通道不得通过普通折扣累计替代；
- 可以共享部分表征，但不能共享同一 Bellman target。

## 12. 有限约束视野

由于无限时域最大值 Bellman 算子不一定是压缩映射，实际训练采用有限约束视野 \(H_c\)：

\[
Q_r^{(h)},\qquad h=1,\ldots,H_c,quad r\in\{s,d\}.
\]

递推应覆盖从当前状态开始的最大违反量，并在终止状态停止 bootstrap。约束 target 不使用折扣。

为降低危险状态下的乐观低估，可采用多个约束 Critic 的保守上聚合，并配合：

- 碰撞和死锁终止状态；
- 多步或多视野监督；
- 危险片段分层采样；
- target 网络；
- 约束预测误差和低估率监控。

这些措施提高经验稳定性，但不把学习型 Critic 变成严格证书。

## 13. 约束策略学习

理论控制问题为：

\[
\begin{aligned}
\min_\Pi\quad
&\mathbb E_{y_0\sim d_0}V_\ell^\Pi(y_0)\\
\mathrm{s.t.}\quad
&V_s^\Pi(y)\le0,\quad\forall y\in\mathcal Y_0,\\
&V_d^\Pi(y)\le0,\quad\forall y\in\mathcal Y_0.
\end{aligned}
\]

需要假设初态集合 \(\mathcal Y_0\) 位于可行域内。动作级策略改进为：

\[
\Pi^+(y)
\in\arg\min_{\mathbf u}Q_\ell^\Pi(y,\mathbf u)
\]

满足：

\[
Q_s^\Pi(y,\mathbf u)\le0,
\qquad
Q_d^\Pi(y,\mathbf u)\le0.
\]

训练采用集中式 Critic 和分布式确定性 Actor。对偶乘子原则上应依赖状态：

\[
\nu_s(y),\nu_d(y)\ge0.
\]

Actor 的原始—对偶目标为：

\[
\mathcal L_{\mathrm{actor}}
=\mathbb E[
Q_\ell
+\nu_s(y)(Q_s+\delta_s)
+\nu_d(y)(Q_d+\delta_d)
]
+\beta_{\Delta u}\mathcal L_{\Delta u}.
\]

全局标量乘子只能作为简化实现或消融，因为它约束的是采样分布下的平均违反量，不能对应状态逐点约束。

## 14. 安全、死锁与任务优先级

优先级固定为：

\[
\boxed{\text{安全}>\text{解除死锁}>\text{任务效率}.}
\]

含义为：

1. 安全和死锁约束共同可行时，同时满足；
2. 二者冲突时，只允许放松死锁约束；
3. 安全本身不可行时，执行最小安全违反或紧急策略，并明确记录不可行状态；
4. 不得在安全不可行时继续宣称安全保证；
5. 任务效率只在剩余可行空间中优化。

实际训练可以用对偶更新、保守 Critic 和可行性近似来体现该优先级，但应把它们称为工程近似。

## 15. 意见如何影响动作

意见作为增广状态的一部分，同时改变三类 Value：

\[
z\rightarrow(Q_\ell,Q_s,Q_d).
\]

因此它既改变任务意义下“哪个动作更优”，也改变约束意义下“哪些动作仍可接受”：

\[
\mathcal U_{\mathrm{feas}}(y,z)
=\{\mathbf u:Q_s(y,z,\mathbf u)\le0,
Q_d(y,z,\mathbf u)\le0\}.
\]

\[
\mathbf u^\star(z)
=\arg\min_{\mathbf u\in\mathcal U_{\mathrm{feas}}(y,z)}
Q_\ell(y,z,\mathbf u).
\]

只有在 Value 对 \((z,\mathbf u)\) 足够光滑、局部解唯一，并满足 LICQ、严格互补和二阶充分条件时，才能由 KKT 隐函数关系得到：

\[
z\mapsto\mathbf u^\star(z)
\]

局部连续可微。活跃约束切换时通常只能主张分段可微或局部 Lipschitz；最优解不唯一时可能发生跳变。

## 16. 四阶段训练方案

### 阶段一：观测与意见语义预训练

训练内容：

- 成对特征和历史表示；
- 风险注意力；
- 观测似然；
- KL 意见更新；
- 反事实校准和单调关系。

目标是先使 \(z\) 具有稳定、可检验的物理语义。阶段结束时验证：

- 求根存在性、唯一性、残差和隐式梯度；
- 意见校准误差；
- 意见与未来风险缓解的正相关；
- 错误初始意见的纠正速度；
- 低风险和高风险下注意力是否分别位于临界点两侧。

### 阶段二：三类 Critic 预训练

固定或极慢更新意见模块，利用覆盖安全、近碰撞、碰撞、正常等待和真实死锁的数据训练：

- 任务 Critic；
- 有限视野安全 Critic；
- 有限视野死锁 Critic。

重点验证三类 Bellman target 是否正确，以及约束 Critic 对未来最大违反量的低估率。

### 阶段三：约束 Actor 训练

保持意见模块冻结，使用原始—对偶目标训练分布式 Actor，并更新状态相关对偶变量。集中式 Critic 可以继续随新数据更新。

重点验证：

- Actor 执行时只依赖本地信息；
- 安全违反不因任务优化恶化；
- 死锁率和持续时间下降；
- 动作连续性和控制限制得到满足；
- 不可行状态被正确识别和记录。

### 阶段四：端到端联合微调

允许长期 Value 梯度通过 Actor 和意见层回传，但始终保留：

- 观测似然损失；
- 语义校准损失；
- 单调约束；
- 曲率硬检查；
- KL 求根残差检查。

联合训练必须从原始序列重建当前历史和意见，避免长期使用旧网络产生的缓存意见。若任务性能提升但意见校准、相关性或单调性明显恶化，应停止并回滚。

## 17. 执行阶段

每个控制周期按以下方法执行：

1. 获取本车和可见邻车局部观测；
2. 更新动态交互图及边身份；
3. 更新每条边的历史表示；
4. 计算当前新证据和风险注意力；
5. 求解每条边的一维 KL 意见更新；
6. 聚合所有有效邻居消息；
7. Actor 输出名义动作；
8. 应用与训练一致的动作幅值和变化率限制；
9. 可选地应用本地保守安全投影；
10. 保存边状态和上一步实际动作。

执行时禁止使用：

- 全局状态；
- 其他车辆内部意见；
- 集中式 Critic；
- 反事实未来标签；
- 在线全局博弈求解；
- 显式通信。

意见求解未收敛时，应使用确定性回退，例如保持上一时刻意见，并记录异常。

## 18. 理论与数值验证

必须验证以下六类性质：

### 18.1 意见有界性

\[
z_{ij,t}\in[-1+\varepsilon_z,1-\varepsilon_z].
\]

覆盖随机输入、极端证据、长序列和边界初值。

### 18.2 冻结观测下的自由能下降

验证连续动力学的理论关系，并验证精确 KL 近端步满足冻结自由能不增加。

### 18.3 分岔

在中性证据下验证 \(A_c=1/a\)，并额外验证加入非中性似然后临界条件由完整 \(\mathcal F''(0)=0\) 决定。

### 18.4 KL 层

验证：

- 解存在且唯一；
- 不同初值求得同一解；
- 求根残差满足阈值；
- 误差界
  \[
  |\hat z-z^\star|\le\varepsilon_{\mathrm{root}}/m;
  \]
- 隐式梯度与有限差分一致；
- 边界解被正确识别。

### 18.5 约束 Value

用可手工计算的短轨迹验证：

- 任务 target 使用折扣加法；
- 安全和死锁 target 使用不折扣最大值；
- 终止状态停止 bootstrap；
- 有限视野含义与实际最大违反量一致。

### 18.6 分布式和对称性

验证：

- 邻居排列不改变聚合结果；
- 车辆标签一致置换时联合策略等变；
- 改变 Actor 不可见的全局信息不会改变其动作；
- 无邻居、新邻居和短时遮挡均有确定行为。

## 19. 实验与消融

必做消融：

1. 去掉意见，仅使用局部观测；
2. 用一般历史隐藏状态替代结构化意见；
3. 用普通 Neural ODE 替代自由能意见层；
4. 用显式离散更新替代 KL 结构保持更新；
5. 去掉语义校准；
6. 去掉单调约束；
7. 将三类 Value 合并为加权普通 Critic；
8. 去掉安全 Critic；
9. 去掉死锁 Critic；
10. 禁止 RL 梯度进入意见模块；
11. 使用全局标量对偶替代状态相关对偶；
12. 去掉约束 Critic 的保守组合。

核心评价指标：

- 碰撞率、近碰撞率、最小距离、安全 Value 违反率；
- 死锁率、持续时间、解除时间、红灯误报率；
- 成功率、平均通行时间、能耗代理、舒适性和 jerk；
- 意见校准误差、意见与未来缓解的相关性；
- 错误初始意见纠正时间；
- KL 残差、边界命中率、计算耗时；
- 未见车辆数量、密度、交互拓扑和感知噪声下的泛化。

所有稀有安全事件应同时报告事件数、总暴露时间或里程以及置信区间，不能只报告均值。

## 20. Codex 实施顺序

Codex 应按下列依赖顺序推进，但自行决定代码组织和具体技术选型：

1. 建立统一环境、数据和增广状态语义；
2. 完成动态交互图、成对特征和边生命周期；
3. 完成历史、证据和风险注意力；
4. 完成自由能、KL 求解和隐式梯度，并通过全部数学验证；
5. 完成反事实标签、校准和单调语义约束；
6. 完成多邻居聚合和分布式 Actor；
7. 完成安全 margin 和死锁增广状态；
8. 完成任务、安全、死锁三类 Critic；
9. 完成三种不同 Bellman target 和有限约束视野；
10. 完成状态相关原始—对偶 Actor 训练；
11. 完成四阶段训练和联合微调回滚机制；
12. 完成确定性部署循环；
13. 完成消融、泛化、数值性质和理论性质验证。

其中第 4 步是硬门槛：KL 意见层的唯一性、求根残差和隐式梯度未验证前，不进入后续控制训练。

## 21. 阶段验收标准

### 意见层验收

- 所有输出有界；
- 曲率条件可检查；
- 求根稳定且误差可量化；
- 隐式梯度正确；
- 意见有明确合作语义且校准有效。

### Critic 验收

- 三类 target 与定义完全一致；
- 约束通道不使用普通折扣回报；
- 死锁不把正常红灯等待判为违反；
- 危险状态的低估率被单独报告。

### Actor 验收

- 执行路径严格分布式；
- 训练和部署使用一致的实际动作定义；
- 安全优先级没有被任务权重覆盖；
- 不可行状态有显式处理和日志。

### 联合系统验收

- 无数值异常和未解释的意见求解失败；
- 联合微调不破坏意见语义；
- 安全指标相对基线不恶化；
- 死锁指标相对无死锁约束基线改善；
- 多随机种子和未见场景下结果稳定；
- 所有失败案例可复现。

## 22. 最终可主张的贡献

完成上述方法和验证后，可以严谨地主张：

\[
\boxed{
\text{系统利用 Bernoulli–Fisher 几何上的 KL 结构保持动力学，
从局部运动历史形成有界、连续、可校准、可纠正的合作意见；
再将意见纳入增广状态，通过任务、安全和死锁三种不同递推结构的 Value，
训练分布式多车控制策略。}
}
\]

保持不变的两条核心公式为：

\[
\boxed{
\tau_z\dot z_{ij}
=-(1-z_{ij}^2)
\partial_{z_{ij}}
\left[
U_{A_{ij}}(z_{ij})
-\lambda_{\mathrm{obs}}A_{ij}
\log p_\phi(\xi_{ij}\mid h_{ij},z_{ij})
\right]
}
\]

以及：

\[
\boxed{
\begin{aligned}
\min_\Pi\quad&V_\ell^\Pi\\
\mathrm{s.t.}\quad&V_s^\Pi\le0,\\
&V_d^\Pi\le0.
\end{aligned}
}
\]

实现中最重要的约束是：不要让工程简化破坏这两部分各自的结构，也不要把学习近似描述成无条件的理论保证。
