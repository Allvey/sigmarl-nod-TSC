# NOD-MARL 安全优先实施方案与新 Session 交接（V2：意见调节屏障约束）

> 更新时间：2026-09-07  
> 用途：新开发 session 的首要阅读材料。本文以当前工作区和实际训练结果为准，区分“已经完成”“当前未提交”“后续计划”，避免重复实现或按旧方案误改。

> 本轮范围调整：用户决定暂缓 Deadlock Critic，优先安全。近期只推进任务 Critic + Safety Critic + NOD/Actor；死锁标签补充、Deadlock ensemble、死锁 Actor 约束及死锁对偶网络均移出实施范围。本文的新顺序替代旧交接计划中的双约束路线；原方法文档保留为长期研究参考。
> 阶段7历史验收记录：根配置和测试入口已停用Deadlock；正违反量权重更新、可靠性门控、诊断及checkpoint迁移已实现，当时39项测试通过、完整训练效果待用户验证。此处保留历史证据，不据此重新把阶段7列为待实现，也不新增碰撞率改善结论。

> V2 规划决定：按用户确认，以“阶段7已完成”为后续开发基线，阶段1—7实现不重做。阶段8起转向“固定物理安全标准 + Safety State Value + 意见调节离散屏障参数 κ + PPO安全伪优势”。本次仅调整文档，阶段8及以后尚未实现；上面的39项测试和训练待验证说明为阶段7历史记录，并非本次重新运行或新增性能结论。不再推进双风险分支显式加权、邻车预测模块或状态相关安全乘子网络。

## 0. 当前状态与近期顺序

| 阶段 | 状态 | 本轮决定 |
|---|---|---|
| 1—4：NOD 与 Actor 接入、稳定性修复 | 主体实现完成，有完整训练记录 | 保留现有结构和标准差上限 |
| 5：Safety Critic | 实现完成，低估问题仍待改善 | 继续使用并验证可靠性 |
| 6：Deadlock Critic | 实现完成，但已有多seed训练没有正样本 | 暂停，不再作为安全工作的前置条件 |
| 7：Safety Actor 约束 | 已完成，作为当前基线 | 保留实现、模型及结果作对照，不重做阶段7 |
| 8：Safety State Value 与固定 κ 屏障训练 | 未实施，下一步 | 先建立可独立验收的 DGPPO 风格安全训练链路 |
| 9：意见调节成对屏障参数 κ | 未实施 | 将每条有效意见用于对应车辆对的约束 |
| 10：机制验证、意见校准与安全可靠性 | 未实施 | 固定 κ / 真实意见 / 消融对照，验证动作及安全效果 |
| 11：汇入覆盖、密度与多seed泛化 | 未实施 | 在一致评估口径下验证目标地图和不同车辆数 |
| 12：动作一致性、危险恢复及受控微调 | 条件性扩展 | 按证据逐项实施，NOD联合微调最后考虑 |

近期完成标准是：意见明确改变相关约束和训练信号，危险低估与碰撞得到控制，同时保留通行能力。阶段8先验证固定参数基线，阶段9只加入意见调节，阶段10判断是否值得推广；不是保证任何状态下不同意见都输出不同动作。以下所有“后续修改”均为计划，未实施时不得当作当前能力。

## 1. 不可改变的项目约束

后续所有阶段都必须遵守：

1. 训练入口保持为 `python main_training.py`。
2. 测试入口保持为 `python main_testing.py`。
3. 配置仍从根目录 `config.json` 读取，不另建一套 CLI。
4. `n_iters` 保持为 `250`。
5. 每个阶段结束时都必须是可以从头训练、保存 checkpoint、再用原测试入口加载的完整版本，不能提交只有半条数据流的中间状态。
6. 不恢复已经删除的 TopologyLearner、TopologyActionPredictor 和 opponent modeling。
7. 不采用此前效果不好的残差式意见接入。
8. 不要求每阶段性能必然高于 base model，但不得用“后面还没实现”解释运行错误、NaN、无法保存或无法测试。
9. 工作区可能包含用户或其他 session 的未提交修改；禁止 `git reset --hard`、`git checkout --` 或覆盖无关文件。
10. 本轮优先级为安全优先、同时报告通行效率；不新增死锁学习目标，不以死锁正样本验收阻塞安全主线。
11. 安全距离、道路边界和碰撞标签不随意见改变。κ的参数映射是控制设计选择，不得把它称为合作概率推出的安全定理。
12. 新旧安全训练模式互斥；保留阶段7作对照，不把旧Q动作梯度惩罚和新屏障伪优势同时加入Actor损失。

## 2. 当前仓库快照

当前 `HEAD`：

```text
76419ac 阶段7 加入Safety Critic
389024a 修复 testing 问题
e0e7ed6 阶段6 3 critic
6d10c8c 阶段5 加入 safety critic
95a88b0 阶段4 效果好了很多
9a2a08d 阶段4
b253aee 阶段4，存在策略坍塌
ae4c159 阶段1-3调整
c0e078a 阶段1-3
```

当前已提交到 `HEAD` 的能力：

- 简化后的 NOD 意见模型及独立训练；
- NOD 消息接入分布式 Actor；
- Actor 方差上限修复；
- 独立 Safety Critic；
- 独立 Deadlock Critic；
- 新旧模型的训练/测试加载修复。

阶段7原型已在76419ac提交；当前工作区是其收尾修改（可靠性门控、正违反量更新和暂停Deadlock），主要涉及：

```text
MODEL_STRUCTURE.md
config.json
utilities/helper_training.py
utilities/mappo_cavs.py
utilities/nod_marl/safety.py
tests/test_safety_training.py
tests/test_safety_constraint.py
main_testing.py（覆盖旧JSON的Deadlock开关）
```

W&B 日志文件也是运行时改动，不应当作为核心源代码处理。

截至本文生成时：

```text
git diff --check：通过
pytest：39 passed
```

测试使用的解释器为：

```bash
/Users/zhangxiaotong/anaconda3/envs/sigmarl-nod/bin/python -m pytest -q
```

用户正常训练和测试仍使用原命令，不需要改成上述绝对路径。

## 3. 当前真正生效的网络结构

### 3.1 NOD 与 Actor

```text
每条有向交互边的 20 维物理特征
  └─ 相对位置、相对速度、朝向、距离、TTC、冲突点、ETA差、重叠风险等
        │
        ▼
共享 GRUCell：20 → 64
        │
        ├─ 6维单调物理风险量
        ├─ 1维风险注意力
        └─ 1维有界意见 z
        │
        ▼
每边 72维 context
        │
        ▼
LayerNorm → 72 → 64 → 32 → 邻居维 masked attention
        │
        ▼
32维 NOD 消息，每维限制在 [-0.1, 0.1]

Actor 输入：
32维原始局部 observation
+ 32维 NOD 消息
+ 2维上一时刻动作
= 66维

共享分布式 Actor：66 → 256 → 256 → 4
                              ├─ loc[2]
                              └─ scale[2]，当前限制 scale ≤ 1.0
                                      │
                                      ▼
                                  TanhNormal
                                      │
                                      ▼
                              二维车辆连续动作
```

Actor 在执行时只使用本地观测、本车可见邻居形成的 NOD 消息和上一动作。集中式 Critic 不参与部署动作计算。

### 3.2 当前代码中的三类 Critic，以及计划保留的主线

```text
任务 Critic
  └─ MAPPO 集中式 State Value，使用折扣加法 GAE/PPO target

Safety Critic
  └─ 全局观测/物理状态 + 当前联合动作
  └─ 预测 h=[1,4,8,16] 内最大安全违反量
  └─ target 不折扣

Deadlock Critic
  └─ 全局观测/物理状态 + 12维时序死锁增广状态 + 当前联合动作
  └─ 预测 h=[1,4,8,16] 内最大死锁违反量
  └─ target 不折扣
```

Safety 与 Deadlock 网络、优化器、随机数流和 checkpoint 相互独立，也不与 NOD/PPO 共享参数。

根配置和测试入口已关闭 Deadlock 分支，主线只保留任务与 Safety Critic。保留现有死锁实现和旧 sidecar 的可选兼容，不进行大范围删除。短程训练验证了没有死锁监督更新或新增死锁checkpoint；旧文件可以存在，不参与当前测试加载。

## 4. 已完成阶段

### 阶段 1—3：NOD 语义链路和行为隔离

已完成的实质内容：

- 从原始物理状态构造稳定的有向交互边和 20 维成对特征；
- 维护 agent identity、generation 和边生命周期，避免重置后串接旧历史；
- 使用 GRU 表达成对历史；
- 构造风险注意力、证据似然、自由能和一维 KL 意见更新；
- 生成反事实软标签，训练 NLL 与校准 BCE；
- 验证意见有界性、求根残差、曲率和边重置行为；
- NOD 参数与 PPO 参数、随机数流和梯度隔离；
- 采集时按时间顺序推进 NOD，PPO replay 只读取 detached context，不乱序重放 GRU。

这部分当前仍在工作，不应重新写一套 topology 或动作预测网络。

### 阶段 4：NOD 消息进入 Actor，并修复方差坍缩

已完成的实质内容：

- 删除旧 topology/action predictor/opponent modeling 训练链路；
- 将 72 维边 context 通过排列不变注意力聚合为 32 维消息；
- Actor 输入改为 `[observation, NOD message, previous action]`；
- 消息聚合器使用独立较小学习率 `5e-5`；
- NOD 默认每 10 个 rollout 更新一次；
- 风险权重限制在 `[0,1]`；
- Actor 的 `scale` 增加固定上限 `1.0`。

曾确认原始状态相关 scale 会从约 `0.3` 膨胀到数百，使随机动作大量饱和到边界并导致车道碰撞。实际提交的修复是 `BoundedNormalParamExtractor`：保留当前状态相关方差结构，只在输出处执行 `scale.clamp_max(1.0)`。

历史上 `NOD_MARL_Actor_Stability_Modification_Plan.md` 记录过“改成状态无关全局方差”的备选方案。该文件在本轮磁盘检查中未找到；该方案没有落地。新 session 应以当前 scale 上限实现为准，不按旧 IDE 标签或备选方案重写 Actor。

### 阶段 5：独立 Safety Critic

已完成的实质内容：

- 环境输出车辆间距、道路边界和碰撞的 signed safety margins；
- `g_s≤0` 表示满足，`g_s>0` 表示违反；
- Safety Critic 输入当前状态和真实执行联合动作；
- 监督目标为有限视野内从当前状态开始的最大违反量；
- 终止、agent generation 变化和 rollout 截断均有有效性 mask；
- 危险低估样本在 Smooth L1 中获得更高权重；
- 在新 rollout 上先记录预测误差，再训练 Critic，避免把拟合后误差当作泛化误差；
- 保存独立 `*_safety_critic.pth`。

阶段 5 不改变 Actor 行为。

### 阶段 6：独立 Deadlock Critic（实现保留，后续工作暂停）

已完成的实质内容：

- 建立持续低速、路线进展、冲突车组、规则许可和局部可解除动作共同定义的时序死锁状态；
- 正常等待、无冲突单车停车和物理不可解除状态不会仅凭低速被标记为策略性死锁；
- 维护 12 维 deadlock augmented state；
- Deadlock Critic 使用独立有限视野最大值 target；
- 保存独立 `*_deadlock_critic.pth`；
- 输出 recall、precision、漏判、误报、持续时间、有效样本等指标。

阶段 6 同样不改变 Actor 行为。

已有多seed训练中，死锁正样本和有效计时均为0，不能认为该网络已学会识别死锁。按本轮用户决定，不再投入死锁场景构造、标签调整、训练或 Actor 接入；这项未验收事项不会阻塞阶段7—9。仍通过速度、停车时长和通行率观察策略是否过度保守，无需为此训练 Deadlock Critic。

## 5. 当前可靠的性能参照

### 5.1 阶段 4—6、未加入 Safety Actor 约束

文件：`outputs/testing_random/reward7.43_data.json`

```text
迭代数：250
最佳奖励：7.427，第157轮
最终奖励：6.087
最后20轮平均奖励：6.044
最后20轮平均总碰撞率：0.00285
```

这证明 `scale≤1.0` 后，当前 NOD→Actor 结构能够完整训练，不再出现早期版本的方差型策略坍缩。后续阶段应以此作为主要行为参照，而不是已经坍缩的 `reward4.84`。

### 5.2 阶段 7 的历史中途快照

历史文件名：`outputs/testing_random/reward5.86_data.json`。最佳奖励文件可能随训练更新而替换；不能假定该路径仍存在，也不能由这个快照推断训练已中断。新 session 应扫描当前 `reward*_data.json`，按配置中的 seed 和 `is_using_safety_constraint` 找到对应运行。

旧版交接文档记录到 `101/250` 轮：

```text
最佳奖励：5.857，第99轮
最近20轮平均奖励：约5.18
```

这是不完整训练快照，不能据此判断最终效果。其权重归零问题在后续读取的123轮快照中仍存在；进一步分析应以实际可读的最新JSON为准。

## 6. 阶段 7：已完成基线及历史验收记录

### 6.1 原型已实现的基础链路

当前工作区已经实现：

- `is_using_safety_constraint` 开关；
- Safety Critic 完成 10 个有效 rollout 后启用约束；
- 每个 PPO minibatch 用当前 Actor 重新计算确定性名义联合动作；
- Safety Critic 参数冻结，只保留 `risk → action → Actor` 梯度；
- h=1 不参与未来动作约束，只使用 h>1；
- 当前已经发生安全违反的状态被排除，首版只学习“从安全状态避免进入危险”；
- 全局标量约束权重及上下界、对偶更新、checkpoint 恢复；
- `actor_constraint_*` 指标；
- 单元测试和最小功能 PPO 测试。

当前损失近似为：

```text
L = L_PPO + lambda_s * mean(relu(Q_s + safety_margin))
```

这是阶段7的工程化标量约束，不是严格安全保证。旧方法曾计划状态相关对偶变量，V2已用第7节的屏障路线替代这一后续目标。

### 6.2 收尾前发现的问题（历史证据）

在阶段 7 的 101 轮训练中：

```text
lambda_s 在第11轮开始生效
lambda_s 在第16轮达到约0.0326
lambda_s 在第50轮下降到0
第50轮后约束损失持续为0
最近阶段预测违反率仍约0.32
Safety Critic 最近20轮 unsafe recall 约0.836
Safety Critic 最近20轮 underestimate rate 约0.237
worst underestimate 最近仍约1.5
```

直接原因是当前对偶权重使用所有 eligible 状态的“有正有负的平均 signed risk”更新。大量负风险样本会抵消危险尾部，因此即使约30%的状态预测违反，平均 risk 仍为负，最终把全局权重推到0。

上述问题促成了本次收尾修改：权重更新已改用正违反量，并补充可靠性门控；原型的历史曲线不能当作修复后的训练结果。Deadlock Actor约束仍在近期范围外。

### 6.3 阶段 7 的收尾修改

以下收尾项已实现；本节保存当时完整训练尚待验证的历史口径，V2按用户确认将阶段7作为已完成基线：

0. **停用 Deadlock 分支**：根配置设为 `is_using_deadlock_critic=false`，保留源码及旧 checkpoint 兼容；核对使用旧训练JSON的测试入口不会无意重新启用该分支。关闭它不代表 Safety 自动可靠，也不改变已有Actor权重。
1. **可靠性门控**：不能只按 `warmup_batches=10` 启用约束；同时要求 Safety Critic 已见到足够危险样本，并满足配置化的 recall/underestimate 条件。门控不满足时继续训练 Critic，但约束 Actor 保持关闭。
2. **危险尾部对偶信号**：标量权重更新不能继续使用全部 signed risk 的普通均值。先采用正违反量 `mean(relu(q))` 与显式风险预算比较的最小方案，预算、上下限和无违反时的更新规则必须写清楚；不同时引入多种尾部风险算法。若后续需要CVaR，作为独立对照再改。避免危险样本被负风险抵消，也监控权重长期顶到上限但碰撞未改善的情况。
3. **区分三种量并分别记录**：普通 signed mean、positive/tail risk、violation ratio。Actor penalty 和对偶更新使用哪一种必须在代码和指标名中明确。
4. **显式梯度隔离断言**：约束反传后 Safety 参数梯度必须为空，Actor（包括消息聚合器）必须能收到非零梯度。
5. **继续保持 scale 上限**：Safety 约束不能替换或绕过 `BoundedNormalParamExtractor`。
6. **checkpoint 兼容**：旧 Safety sidecar 缺少新门控/对偶状态时重新执行可靠性预热，不影响 policy/critic/NOD 加载。
7. **有效状态与启用状态可解释**：分别记录门控就绪、权重是否为正、eligible样本数量和实际非零约束损失。使用新数据上拟合前的统计或独立验证数据做门控，避免用刚拟合的训练误差决定可靠性；门控长期不通过时报告原因并转入阶段8改进Critic，不放宽指标掩盖问题。

当前具体实现：门控统计最近5批拟合前的“当前安全且所有未来视野目标有效”的状态，先对h>1取最大值；危险目标数至少64、recall≥0.90、低估超过0.05的比例≤0.15，且至少完成10批监督训练。当前批使用此前已完成批次的门控证据。权重更新为 `clip(lambda + dual_lr * (mean(relu(q)) - risk_budget), 0, max_weight)`，预算默认0；没有违反时保持权重，不再被负风险抵消。已有危险且门控就绪时，零权重可恢复；有上限仍不等于安全保证。

`constraint_gate_reason`：0就绪、1开关关闭、2预热、3危险目标不足、4召回不足、5低估过多。`actor_constraint_ready`表示门控就绪，`actor_constraint_active`表示本批确有正权重的非零惩罚。分别记录signed risk、positive risk、dual signal、样本数量和权重顶上限状态；门控关闭时sample_count=0，风险字段为0不表示无危险。门控统计与原来的全体/跨视野unsafe_recall口径不同。

Safety sidecar的版本2约束合同记录门控配置、预算、权重设置、预热批数及近期统计；继续训练仅在合同一致、状态齐全时恢复，否则重新可靠性预热并恢复初始约束权重。兼容的Safety网络及优化器可保留，Actor/NOD加载不受影响。功能测试覆盖拟合前统计、门控恢复和关闭、权重归零回归、Actor/消息聚合器非零梯度以及Critic梯度隔离。

阶段7历史训练验收清单如下，供后续基线对照使用，不是V2新增的阶段7实施任务：

- 无 NaN/Inf、无动作方差坍缩；
- 约束启用条件和关闭原因可从指标解释；
- 当危险尾部持续为正时，`lambda_s` 不应长期为0；
- Safety 低估率、碰撞率和通行奖励同时报告；
- policy、任务/Safety Critic 和 NOD checkpoint 能正常保存、兼容加载；缺少或停用死锁 sidecar 不阻塞入口。Safety 与车辆数不兼容时必须明确报告，不能将随机初始化评价当成安全控制能力。
- 通行率、停车时间和碰撞暴露量同时报告，不能通过全体停车获得表面上的安全改善。

## 7. V2 后续实施：意见调节屏障约束

### 7.1 统一设计合同（阶段8—12均遵守）

本节是后续实现的规范；第3、6节仍描述已完成的阶段7代码。不要把两种结构混为当前能力。

#### 符号、作用路径与范围

- `i`为本车，`j`为相关邻车，`t`为控制步；`b_i,t`为本车当前局部观测及所需历史信息。
- `z_ij,t ∈ (-1,1)`为本车根据历史对邻车将采取风险缓解行为的意见；不是本车的合作指令。
- `g_ij,t`为固定物理定义的成对安全违反量；非正表示满足。道路、实际碰撞等约束分别保留固定物理定义。
- `V_ij,t = V_s(b_i,t, j)`为沿当前确定性策略继续执行时的成对安全状态价值估计，输入不含当前待选动作。共享编码器可利用其他相关邻车信息，不假定每对车物理上相互独立。
- `κ_ij,t`为每控制步的无量纲屏障参数，`0 < κ_min ≤ κ_ij,t ≤ κ_max < 1`。
- `C_ij,t`为屏障违反量；`P_i,t`为本车有效约束的最大正违反量；`ν>0`为安全伪优势的训练系数，独立于意见。

保留两条作用路径：

```text
局部观测/历史 → NOD → z → 现有消息聚合 → Actor动作分布
                        └→ κ_ij → 成对屏障违反量 → 安全伪优势 → PPO更新Actor
物理状态/历史 → Safety State Value ───────────┘
```

第二条路径不依赖Safety网络学会使用z：固定Value输出，改变z仍会改变约束参数。Safety是否额外接收意见属于另一个消融，不是首版成立的前提；不同时增加预测模块或合作/不合作双风险分支。

#### 屏障公式与PPO符号

对当前评价为安全且身份连续的有效车辆对：

```text
κ(z) = κ_min + (κ_max - κ_min) * (z + 1) / 2
C_ij,t = V_ij,t+1 - (1 - κ_ij,t) * V_ij,t
P_i,t = max(0, 所有有效成对C, 固定κ的道路/其他约束C)
```

当`V_t<0`时，较小κ要求保留更多安全余量；较大κ允许更快接近`V=0`，但公式仍要求下一步不跨越零边界。`V_t=0`时意见不能放宽边界。真实已危险或`V_t>0`状态走独立恢复/固定安全规则，不套用“正意见可放松”的逻辑。

这里的κ是离散系数。DGPPO代码使用`(V_next - V)/dt + alpha*V`，对应`κ=alpha*dt`；不得把连续时间alpha数值直接抄成κ。先通过阶段8确定适用于当前dt的固定`κ0`，阶段9的区间以它为中点，即`κ_min + κ_max = 2κ0`，确保中性`z=0`与固定基线一致；初始只做小范围调节，不把对话中的0.2/0.8示例当默认超参数。

奖励最大化的首版伪优势采用：

```text
A_safe_i,t = 1[所有有效C≤0] * A_task_i,t - ν * P_i,t
```

`A_task`为原任务GAE，指示函数满足条件时为1；任务GAE按原口径归一化后再合成，不再次对合成优势做普通去均值，避免改变危险样本的符号。安全部分进入Actor的PPO surrogate；任务Value仍使用原始任务回报目标。Critic门控未就绪或转移无效时回退原任务PPO，单独记录原因；不能把这类样本报告为“安全”。

每个rollout的Value、κ、mask、伪优势在PPO epochs前计算并detach，epochs内不随新Actor参数重算。安全路径通过log-prob/PPO更新Actor和消息聚合器，不沿`∂Q/∂action`或`∂C/∂z`反传。NOD仍按有序数据工作。

`ν`先固定、可配置，不直接继承阶段7的自适应lambda。若之后测试论文式ν日程，必须单独对照。若使用额外数值安全裕度，固定/意见模式必须同值并在门控、mask及日志中使用一致口径；首版公式与实现的差异必须写明。

#### 安全含义和意见映射的边界

- 物理标签与零边界不随z变化；本方案调节的是约束保守程度。正意见不等于邻车确实会让行。
- 仅在精确Value、有效屏障条件、可行初态和逐步满足约束等条件下，`V_next≤(1-κ)V≤0`才支持相应不越界结论；有限数据、局部观测、bootstrap及PPO训练不提供这些保证。
- 约束松弛、`V=0`或两个动作同样安全时，意见可以不改变动作。必须验收的是相关活跃约束中的合理影响，而非全状态非零动作敏感度。
- 一辆车的正意见不能放宽另一辆车或道路边界的约束。先逐边计算违反量，再做最坏违反量聚合；不得先平均所有z再调全局安全条件。
- 不通过增大κ区间、放宽可靠性门控或奖励z变正来制造“意见有效”。

### 阶段 8：Safety State Value 与固定κ屏障训练

**目的：** 建立新约束所需的基础链路，暂时不让意见调节κ。阶段7是完整对照模式，不修改其已有语义。

当前Safety是全局联合动作Q，四个输出代表`h=[1,4,8,16]`视野，不能直接作为相邻时刻可比较的成对State Value。阶段8是必要的语义迁移，不是一行公式替换。

按8A、8B两个可运行增量完成，每个增量保留入口和保存/加载能力：

**8A：成对物理标签与State Value监督（影子模式）**

1. 增加带稳定`agent id + generation`的成对安全量，保留原安全距离及归一化口径；道路约束按车独立，无法归因到具体邻车的实际碰撞标志使用独立固定规则，不随任意z变化。
2. 使用共享小型成对网络和masked局部聚合输出`V_ij`，另输出每车道路等Value；去掉“当前待选联合动作”的输入依赖。不一次性重写Actor或移植整套Graph Transformer。成对输出轴与原来四个视野头必须明确区分。
3. 使用独立确定性策略采样（Actor mode）监督Safety；原随机rollout继续用于任务MAPPO。两条采样链各有环境、历史、NOD状态和重置生命周期，不把确定性步写入随机collector的缓存；不消耗PPO更新迭代数以外的配置含义，额外采样量单独记录。
4. 采用DGPPO风格的折扣max递推近似长期最大违反量，按每项物理约束独立生成目标。首版单步形式为`Y_t = max(g_t, (1-γ_s)*g_t + γ_s*stopgrad(V_target(b_next)))`，其中`0<γ_s<1`为安全目标折扣，与κ、ν分开配置。多步目标按相同递推倒序展开；非终止片段末端用`max(g_T, stopgrad(V_target(b_T)))`自举，真实终止以终止物理违反量结束、不接续新episode。`V_target`是冻结的目标网络副本，更新节奏记录进合同。不得把rollout截断当作安全终止；不把旧四个有限视野头直接相减当成无限时域屏障。
5. 折扣max是训练近似，不是普通累计cost，也不等于精确无折扣未来max。明确这一差异及低估风险；不能据此继承精确屏障保证。原有限视野真实max保留为预测校验：检查已观测危险的漏判/低估，不能仅凭窗口未碰撞认定长期Value误报。用短轨迹手算验证递推、终止和安全负值可学习；尤其避免无折扣max自举从零初始化后长期停在零、导致κ失去作用。更改γ_s或目标规则必须作为独立实验并重置相应门控证据。
6. 对新边、邻居换位、离开感知范围、generation变化分别建mask。只有同一物理车辆对且当前/下一步Value可有效计算时才能计算C；缺少下一步观测不填0、不当作已安全。监督标签若能按稳定ID跟踪真实后续可继续使用，但不得把不可用的下一步信息泄漏给Actor。
7. 新Safety使用独立模式/版本合同和sidecar；旧Q权重不能按形状近似强装到新V。已有Actor、任务Critic与NOD可兼容加载，新Safety重新监督预热。影子模式不影响Actor，保留完整运行和评估。

**8B：固定κ接入PPO**

1. 新V可靠性达到预设门槛后，按7.1节在真实随机转移上计算Value变化和安全伪优势；全部成对约束用同一个固定`κ0`，道路使用记录清楚的固定参数。
2. 新模式停用阶段7的Q动作梯度惩罚和lambda更新；旧模式完整保留作对照。新Value门控不能恢复旧Q的门控统计，必须按新target、mask和评估口径重新累积。
3. 保留原task Value/GAE、Actor输入、scale上限及优化器主要设置；先固定ν，不同时引入乘子网络。
4. 使用明确的互斥模式，例如计划配置`legacy_q / barrier_fixed / barrier_opinion`（名称为拟议接口，尚不存在）。checkpoint合同记录模式、Value类型、物理标准、dt、κ、ν、目标规则、门控和身份schema；不兼容时给出清楚的加载/预热行为。

**验收：**

- 正/负/零边界与PPO奖励符号正确；安全约束可产生Actor梯度，Safety与NOD不收到此梯度；原任务Value目标未污染。
- 连续车辆对正确配对，重置和窗口边界不串联；随机/确定性状态和RNG隔离，原模式可回归。
- 新V先拟合前评估再监督更新，门控可解释；固定κ下碰撞、道路违反、通行及长停指标完整记录，不以奖励峰值单独判断。
- 检查安全Value是否塌缩到零、相邻Value差是否噪声主导，以及κ改变判定的有效样本覆盖；单独的危险recall不能替代这些屏障信号检查。
- 从头训练、保存、续训、旧policy加载和`main_testing.py`均可运行；未兼容的Safety加载不伪装成可用。
- 由用户执行完整250轮训练。若固定κ模式本身不稳定，先修复阶段8，不用意见调节掩盖问题。

### 阶段 9：z调节对应车辆对的κ

**目的：** 在阶段8稳定基线上只增加`z_ij → κ_ij`这一项机制，不新增Critic、不改安全标签。

1. 实现7.1节有界单调映射；使用动作执行前缓存的当前`z_ij,t`，不使用下一步意见决定当前约束。使用稳定ID对齐成对Value、意见、mask和下一状态。
2. 每条有效车辆对独立使用对应z，之后才聚合违反量；道路、未归因碰撞、无有效意见和危险恢复采用固定/预设保守规则。意见不可用时使用保守κ，另行记录；真正有效的中性z=0映射κ0，不将“缺失”和“中性”混为一类。
3. 固定κ模式继续可用；阶段9的对照从同一个阶段8 Actor/NOD快照出发，NOD语义模块在这组机制对照期间冻结，仅训练Actor、消息聚合器和Safety。更换意见版本时明确重做校准与Safety预热证据；PPO不能通过把z推高来放松约束。
4. 收集`κ分布、意见有效率、各边C、正违反量、控制最大违反的车辆对、任务mask比例、实际安全更新比例`；分别报告道路约束与成对约束，防止道路风险长期主导而掩盖意见路径。
5. 记录同一转移在固定κ与意见κ下的C差异和约束判定改变比例。另记录Actor动作均值/分布变化，不能把“公式改变”当作“行为已经改变”。

**验收：**

- 固定V_t、V_next，只改变相关z，C按公式变化；在V_t<0时较高z不增加这一条C。V_t=0时所有z边界相同。
- 改变无关车辆对意见不改变另一条C；邻居换位后结果按ID等价；终止/新边缺失有明确处理。
- κ范围合法，中性模式复现κ0；缺失意见不自动映射最宽松κ；道路参数完全不变。
- 真实训练中存在由意见改变的有效约束和PPO训练信号，Actor可在相关场景学出行为差异；不要求无冲突或紧急制动状态也出现差异。
- 保留可靠性门控、独立checkpoint以及阶段8固定κ回退模式。

### 阶段 10：意见作用机制、校准与Safety可靠性验收

**目的：** 判断意见带来的差异是否有正确语义并改善策略，避免只是增加保守度或制造动作差异。

按顺序进行，不同时添加多种稳定化机制：

1. **训练对照：** 阶段7旧Q基线、阶段8固定κ、阶段9真实z调节。固定/真实z的Actor直接意见输入保持相同；采用相同初始快照、训练seed组、预算和评估集。分别报告环境步数与耗时，确定性额外采样计入预算说明。
2. **两条路径消融：** 缓存同一物理状态/历史，分别进行“固定Actor意见输入，仅改变约束用z”“固定约束κ，仅改变Actor消息中的z”“两者一致改变”。前者是约束/训练信号诊断，部署时不调用Critic，所以不能期待它立即改变已训练Actor输出。
3. **语义检查：** 加入全中性、打乱z或反号z的诊断；区分固定轨迹上的公式检查与重训/闭环控制实验。任意替换z可能离开数据分布，只作为敏感度实验，不能宣称真实反事实或因果收益。最终以真实历史生成的意见和真实闭环结果判断。
4. **校准：** 按场景及风险区间报告NOD的Brier/NLL、正意见后的实际风险缓解频率、错误正意见纠正速度，尤其检查“邻车实际不让行但z为正”的碰撞情况。已有恒速参考标签是行为代理，不视作严格合作因果真值。
5. **Safety数据：** 覆盖安全、近碰撞、碰撞和错误乐观意见样本；按约束类型、车辆对、预测范围报告新数据拟合前漏判/低估及样本量。门控长期关闭时先改善样本与模型，不降低门槛制造启用率。
6. **必要时才增加保守模型：** 单模型持续低估且数据问题已排除时，独立尝试第二个Safety成员和上聚合，完整对照误报、停车及泛化；不把ensemble作为阶段9前置要求，也不引入预测模块。

**通过条件：** κ确实影响活跃约束；Actor在相关真实交互中出现可解释差异；相对固定κ没有不可接受的安全退化且存在通行/安全收益。碰撞、暴露量、长停和通行共同报告。若只是预测公式变了但行为/收益没有改善，则保留固定κ并定位原因；不扩大κ范围强迫出效果。意见校准失败时先修复语义，不能继续信任正z放宽约束。

### 阶段 11：目标场景覆盖、车辆密度与多seed泛化

**目的：** 在机制验收后解决4车交叉口训练与8车匝道测试的差异。

1. 先在原4车场景保留阶段8/9对照。历史`cpm_scenario_probabilities=[1,0,0]`只覆盖交叉口，实施时重新核对实际配置；`CPM_mixed`名称不意味着训练过汇入。
2. 再增加CPM汇入比例并单独记录，保留交叉口回归；CPM汇入与`on_ramp_1`区分。若在目标地图训练，另设未参与训练的初始条件及地图用于评估。
3. `on_ramp_1`按4→6→8车增加密度；阶段8共享成对V应避免固定全局flatten维度，但结构兼容不等于新密度下可靠，仍需按密度检查校准/门控。旧阶段7固定维度Q不跨车辆数强行复用。
4. 至少3个固定训练seed和共同评估seed集；分别评估最佳/最终模型、确定性/随机动作。报告κ改变判定的比例、意见校准、Critic低估、车辆碰撞/越线事件及车辆步数、通行率、平均速度和长停比例。
5. 正常驶出和碰撞重置分开统计；不把不同暴露量下的原始碰撞次数直接比较。安全提升不能仅来自全体停车。

**验收：** 收益在多seed及目标密度下可复现，意见路径没有在新地图系统性放大错误信任；失败时回退固定κ并保留原始轨迹诊断。

### 阶段 12：条件性扩展（按问题逐项推进）

阶段8—11完成前不为追求复杂度新增网络。12A、12B、12C各自独立验收，后项不是前项交付的前置条件。

**12A：动作一致性与变化率**

若存在动作抖动或随机动作风险，分别处理。采集、物理转移、Safety标签和PPO log-prob对应真实执行动作；不能采样后平滑/裁剪却继续用修改前概率。previous action输入并不等于变化率限制。保持scale上限。

**12B：危险状态与不可行约束恢复**

先诊断真实已违反、Value预测已危险及无可行动作的情况，再设计固定安全恢复/后备机制；这些状态不因正意见降低恢复要求。若添加运行时干预，明确本地可获得的信息，分别评估Actor和完整系统、记录干预率，不用测试过滤掩盖训练问题。

**12C：NOD微调（最后考虑）**

仅在校准稳定且有证据表明冻结NOD限制效果时进行。维持有序序列训练、校准/BCE/NLL、单调性和KL数值检查，独立低学习率并可回滚。首版仍detach用于κ的z，不开放通过增大z来减小安全损失的梯度；若未来研究该梯度，必须另立方案证明不会破坏合作语义。不得恢复死锁目标或状态相关安全乘子网络作为默认扩展。

### 7.2 实施优先级与参考

下一项授权实施时从阶段8A开始，完成影子监督后再接8B。阶段9必须以固定κ链路可运行、可靠性门控有证据为前提；阶段10决定是否扩展到阶段11。阶段7不再作为待实现阶段。

本设计参考DGPPO的State Value/确定性安全采样/PPO伪优势，以及可调屏障变化率的思路；不声称是原论文已验证的意见机制：

- [DGPPO论文](https://arxiv.org/abs/2502.03640) 与 [官方训练代码](https://github.com/MIT-REALM/dgppo/blob/51b3b11c42760cd62f502f9d60cf6d302f413973/dgppo/algo/dgppo.py)。
- [Rate-Tunable Control Barrier Functions](https://arxiv.org/abs/2303.12966)。意见到κ的语义映射、可行性和实证效果属于本项目新增验证范围。

## 8. 后续暂时不要做的事情

- 不恢复Deadlock开发、TopologyLearner、opponent modeling或邻车预测模块。
- 不采用“合作/不合作”两个风险分支按z显式混合。
- 不让z修改碰撞标签、安全距离、道路边界或无关车辆对的约束。
- 不将阶段7的动作Q当成State Value直接做相邻时刻差分，不混用不同视野头。
- 不同时运行旧Q动作梯度惩罚与新屏障伪优势，不新增状态相关dual网络。
- 不删除原始32维观测、不恢复残差式意见接入、不移除scale上限。
- 不因Actor输入66维而无证据地压缩Actor，也不为了模仿论文一次移植完整GNN。
- 不在随机rollout中覆盖确定性NOD状态，不在打乱的PPO minibatch里重放GRU。
- 不用测试时过滤或未计入log-prob的动作后处理掩盖问题。
- 不把“z改变公式”当作“z改善策略”，不把精确屏障推导当成学习系统安全证书。

## 9. 关键文件导航

| 文件 | 当前职责 |
|---|---|
| `NOD_MARL_Methodology_Plan.md` | 方法背景与V2范围入口；旧Q/对偶及死锁路线不作为当前后续清单 |
| `MODEL_STRUCTURE.md` | 当前实际网络结构和已实现阶段说明 |
| `utilities/nod_marl/interaction.py` | 20维成对物理特征和有向边 |
| `utilities/nod_marl/opinion.py` | NOD 历史、似然、风险注意力和意见求解 |
| `utilities/nod_marl/trainer.py` | NOD 在线状态、序列训练、checkpoint 和指标 |
| `utilities/nod_marl/policy.py` | NOD 消息聚合与 66维 Actor 输入 |
| `utilities/nod_marl/safety.py` | Safety Critic、有限视野target、正违反量Actor约束与可靠性门控 |
| `utilities/nod_marl/deadlock.py` | 已有死锁实现，当前入口已停用，保留兼容 |
| `scenarios/road_traffic.py` | 环境 observation、NOD/Safety/Deadlock 物理字段 |
| `utilities/mappo_cavs.py` | Actor/Critic 构造、PPO、三类辅助训练和保存主流程 |
| `utilities/helper_training.py` | Parameters、SaveData、collector、checkpoint 辅助逻辑 |
| `config.json` | 保持原接口的全部开关和超参数 |
| `tests/test_safety_constraint.py` | 阶段7梯度、门控、权重和恢复测试 |

## 10. 新 Session 开始工作的建议顺序

1. 读第0节和第7节：阶段7已完成；本次只有V2规划，后续代码尚未实现。`MODEL_STRUCTURE.md`仍用于查看当前实际结构。
2. 执行`git status --short`并保护已有未提交改动，不能为实施新方案回滚阶段7。
3. 对照`safety.py`的旧动作Q、target及门控，与`mappo_cavs.py`的PPO数据流，确认8A需要的成对标签、独立采样及新V接口。
4. 用户要求开始后，从8A的小范围完整改动实施；不要直接跳到κ公式，也不把旧收尾工作重复列为阶段7待办。
5. 每个增量完成后运行针对性测试与保存/加载smoke；39 passed是阶段7历史基线，不是新模式的测试结果。
6. 用户手动执行完整训练；入口仍为`python main_training.py`和`python main_testing.py`，`n_iters=250`保持。
7. 更新本文件的阶段状态时，区分代码完成、功能测试、完整训练和多seed结论，不提前标完成。

## 11. 交接定位

当前实现基线是已完成的阶段7；V2是用户批准方向后的后续修改计划，本次没有修改训练代码、配置或模型。

```text
阶段7：已完成，保留旧Q约束作对照
  → 阶段8：成对Safety State Value + 固定κ屏障PPO
  → 阶段9：z逐边调节κ
  → 阶段10：机制消融、意见校准、Safety可靠性
  → 阶段11：汇入覆盖、密度与多seed验收
  → 阶段12（按需）：动作一致性 / 危险恢复 / 受控微调
```

核心验收问题是：固定物理安全定义后，意见是否通过对应约束影响策略，并带来可复现的安全与通行收益。下一步从阶段8开始，不引入预测模块，不重做阶段1—7。
