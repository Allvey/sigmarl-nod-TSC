# 单车循迹对照

手动执行：

```bash
python utilities/evaluate_single_car.py
```

默认加载 task_only 的 reward4.57 与 v2 的 reward6.38，测试 intersection_2 的路径 1、5。每条路径用 3 个起点和 2 个初始速度，共 12 个配对样本，每个最多 600 步。起点来自路线中心线内部点，初始朝向沿路线切线，速度为车辆最大速度的 30% 或 70%。这不是原多车碰撞率评估，而是隔离交互影响的诊断。

指定检查点与其他路径：

```bash
python utilities/evaluate_single_car.py --data outputs/dgppo_task_only/reward4.57_data.json outputs/dgppo_minimal_v2/reward6.38_data.json --routes 0 1 2 3 4 5
```

切换场景时同时指定合法的零起始路径编号，例如 `--scenario roundabout_1 --routes 0`。`--fractions` 指中心线内部可选点区间的起始比例，默认 `0 .15 .35`；`--speeds` 为最大速度比例。每个模型使用同一组确定性初始状态、相同 seed；脚本检查两组初始物理状态及 dt 是否一致。

为兼容既有检查点，保留训练时的车辆槽位和输入维度。仅 agent 0 行驶，其余占位车辆放置在远离路网的位置，动作清零，并启用现有距离掩码编码。本车动作不作安全过滤。脚本拒绝 NOD Actor 和优先级策略；当前 task_only、v2 均可使用。

单车诊断期间只使用距离掩码，临时关闭额外的车道归属掩码，避免初始化时尚无车道归属信息而报错。所有占位车辆仍由距离掩码屏蔽；普通训练和多车评估的掩码逻辑不变，诊断退出后临时覆盖自动恢复。

每个样本只统计首次边界碰撞、驶出入口/出口或超时前的数据，不重生；结束后的车辆停止并不再计入。初始车身若已触碰边界则报错，需调整起点。正常驶出出口、误驶出入口、碰撞及超时分别统计，超时不算成功。

输出到 `outputs/single_car_comparison/<时间戳>/`：

- `comparison.json`：两个模型的结局计数、分路线统计和初始状态一致性检查结果。
- `model_0/`、`model_1/`：各自的 `summary.json`、`cases.csv` 和 `trajectories.csv`。轨迹包含位置、朝向、速度、偏离路线距离及物理动作。

优先比较同一 case 的结局与轨迹，其次比较偏离路线及速度。高碰撞模型的统计轨迹更短，不能单靠平均偏差判断好坏。这个实验仅检验相同无邻居输入条件下的循迹差异；原训练配置没有启用距离掩码，因此空邻居输入也可能带来分布变化。即使单车有差异，也不能单凭本实验断言具体是哪个安全损失环节导致。训练和仿真由用户手动执行，本轮仅进行了静态检查。
